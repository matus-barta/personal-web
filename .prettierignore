# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Vendored agent skills, managed by the `skills` CLI. Their markdown carries
# deliberately-broken code samples that Prettier's parsers reject.
/.agents/
skills-lock.json
